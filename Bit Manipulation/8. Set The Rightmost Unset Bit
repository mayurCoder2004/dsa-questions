public class Solution {
    public static int setBits(int N){
        // Write your code here.
        if ((N & (N + 1)) == 0) {
            return N; // No unset bit to set
        }
        
        return N | (N + 1);
    }
}
