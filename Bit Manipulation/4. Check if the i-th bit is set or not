class CheckBit {
    static boolean checkKthBit(int n, int k) {
        // code here
        if ((n & (1 << k)) != 0) {
            return true;
        }
        return false;
    }
}


class CheckBit {
    static boolean checkKthBit(int n, int k) {
        // code here
        int mask = 1;
        int i = 0;
        
        while (i < k) {
            mask<<=1;
            i++;
        }
        
        return (n & mask) != 0;
    }
}
