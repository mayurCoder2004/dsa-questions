class Solution {
    public boolean checkSet(int n) {
        return (n & (n + 1)) == 0;
    }
    public int smallestNumber(int n) {
        int i = n;
        
        while (i <= Integer.MAX_VALUE) {
            if (checkSet(i)) return i;
            i++;
        }
        return -1;
    }
}
